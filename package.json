{
  "name": "daks",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ rube-de/daks",
    "create-local": "graph create --node http://localhost:8020/ rube-de/daks",
    "remove-local": "graph remove --node http://localhost:8020/ rube-de/daks",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 rube-de/daks"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
